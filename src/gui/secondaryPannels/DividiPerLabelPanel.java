package gui.secondaryPannels;

import javax.swing.*;

public class DividiPerLabelPanel extends JPanel {
    public DividiPerLabelPanel(){
        JLabel dividiPer = new JLabel("Dividi per:");
        dividiPer.setHorizontalAlignment(SwingConstants.CENTER);
        add(dividiPer);
    }
}
